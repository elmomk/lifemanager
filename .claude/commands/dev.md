Start the development environment for Life Manager.

Run the following steps:
1. Start Tailwind CSS watcher in background: `npm run tailwind`
2. Start the Dioxus dev server: `dx serve`
3. Report the URLs: local at http://localhost:8080/lifemanager and via Tailscale proxy at port 7000
